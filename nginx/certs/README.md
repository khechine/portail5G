# Certificats SSL

Copier les fichiers suivants dans ce dossier sur le serveur :

- `fullchain.pem`     : certificat site + certificat intermédiaire
- `topnetpro.tn.key`  : clé privée

Source sur le serveur : `/home/master/portail5GV1-prod/nginx/`

Ces fichiers ne sont PAS versionnés (voir .gitignore).
